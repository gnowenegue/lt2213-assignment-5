# LT2213 Assignment 5

This repository contains the materials for Assignment 5 of LT2213 Computational Semantics.

## Setup

1. **Create a virtual environment:**

   ```bash
   python -m venv .venv
   ```

2. **Activate the virtual environment:**

   ```bash
   source .venv/bin/activate
   ```

3. **Install dependencies:**

   ```bash
   pip install -r packages.txt
   ```

4. **Set up IPyKernel:**

   ```bash
   python -m ipykernel install --user --name=lt2213-assignment-5 --display-name "LT2213 Assignment 5"
   ```

5. **Install pre-commit hooks:**

   ```bash
   pre-commit install
   ```

## Development

Ensure you select the **LT2213 Assignment 5** kernel. In VS Code, you can do this by clicking the kernel name in the top-right corner of the notebook editor and selecting it from the list.
